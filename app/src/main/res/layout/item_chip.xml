<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content" android:layout_height="wrap_content"
    android:orientation="horizontal" android:gravity="center_vertical"
    android:background="@drawable/bg_chip" android:layout_marginEnd="6dp"
    android:layout_marginBottom="6dp" android:paddingStart="12dp" android:paddingEnd="2dp"
    android:paddingVertical="2dp" android:minHeight="32dp">

    <TextView android:id="@+id/tv_chip" android:layout_width="wrap_content"
        android:layout_height="wrap_content" android:textSize="13sp"
        android:textColor="@color/glass_text_primary" android:maxLines="1"
        android:ellipsize="end" android:maxWidth="160dp"/>

    <ImageButton android:id="@+id/btn_chip_del"
        android:layout_width="28dp" android:layout_height="28dp"
        android:layout_marginStart="4dp"
        android:padding="6dp"
        android:scaleType="fitCenter"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:tint="@color/glass_text_secondary"/>
</LinearLayout>
